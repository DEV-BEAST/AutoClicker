This is a simpe auto clicker that is activated by home key and exited by the end key.
The standard edition allows you to use the autoclicker with message boxes for instructions.
The simple edition allows the same functionality without the boxes if it gets to annoying to use.
This auto clicker is setup so there is no ui so its set and forget.
